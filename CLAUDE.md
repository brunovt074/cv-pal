See `AGENTS.md` — it is the single source of truth for agent behavior in this repository.

This file exists only so Claude Code auto-loads project context; do not duplicate content here.
Also read `PROJECT_STATUS.md` for current implementation state, and check engram
(`mem_search`/`mem_context`) for prior decisions before starting work.
